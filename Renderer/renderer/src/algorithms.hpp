#include <vector>
#include <limits>
#include <algorithm>
#include <random>




template<typename T>
inline T Max(T left , T right)
{
return left > right ? left : right;
}

template<typename T>
inline T Min(T left, T right)
{
return left < right ? left : right;
}

